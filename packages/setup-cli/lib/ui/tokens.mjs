/** Fireworks brand — banner art, OAuth SVG (truecolor hex). */
export const BRAND = {
  purple: "#6720FF",
  glow: "#A87FFF",
  deep: "#501CBE",
  violet: "#7C3AED",
  magenta: "#9333EA",
};

/** Semantic terminal roles — mapped in theme.mjs to ANSI slots, not hex literals. */
export const SEMANTIC = {
  interactive: "cyan",
  success: "green",
  muted: "muted",
  spend: "orange",
};

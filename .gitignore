# Scratch / test SQLite DBs (Cursor state.vscdb snapshots used during dev)
*.vscdb

# Local FireConnect CLI state (mirrors ~/.fireconnect when run from this repo)
.fireconnect/

# Node
node_modules/

# macOS / editor junk
.DS_Store

# fireconnect demo — generated output + local scratch (any dir, not just root)
fireconnect-demo/
debug.log

# Local demo measurement output (not source)
fc-measure/
fireconnect-demo/
fc-measure-run.sh
